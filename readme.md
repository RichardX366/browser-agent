# Browser Agent

An extension that allows you to control your browser using an AI agent.

## Zipping

To zip the extension, run the following command in the terminal:

```bash
tar -r browser-agent.zip . -x "*.git*" "node_modules/*" "dist/*"
```
